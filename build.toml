[project]
name = "cppmake"
version = "0.0.2"

[build]
default_toolchain = "default"

[toolchains.go.default]
run = "go build"
direct = true

[[target]]
name = "cppmake"
type = "binary"
lang = "go"
src = ["cmd/cppmake/main.go"]
outfile = "build/cppmake"
