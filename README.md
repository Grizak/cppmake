# CppMake

This is just a hobby tool I wrote to try to make a correct build system that doesn't use annoying syntax or unreadable semantics. You write the config in toml and it spits out build.ninja, Makefile or whatever other backend you want. (See [here](test/c/build.toml) for an example)
